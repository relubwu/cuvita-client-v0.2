<form-header header="{{ localePackage.header[locale] }}" description="{{ localePackage.description[locale] }}" />
<form fields="{{ fields }}" action="{{ localePackage.submit[locale] }}" bind:submit="onSubmit" />